import { createSlice } from "@reduxjs/toolkit";

//Esto es un objeto
export const taskSlice = createSlice({
    name: "task",
    //Estado inicial
    initialState: [],
    //podemos crear funciones para actualizar el estado
    // funciones como: añadir, eliminar, actualizar, etc
    reducers: {
        
    }
})